groups:
- name: app-alerts
  rules:
  - alert: ServiceDown
    expr: service_up == 1
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "dservce is down or up"
      description: "monitor detected service down"
